# Dev Core（开发高频纪律）

> 自动适用于所有代码改动，但只负责已确认产品语义之后的实现方法，不负责决定产品应该接受、拒绝或保留什么。下游消费点：`~/.codex/AGENTS.md` §3 / §4 / §6（精简版 + 指针）。本文件是唯一权威，仅当核心条目增删时更新那边。

## 先定语义，再动手
- 先从相关代码、真实运行、测试和仓库稳定现状文档复原当前行为；同类问题 Grep 既有解法，有基线测试 / 构建命令先跑一遍确认现状。代码与文档冲突时报告，不任选一边凑结论
- 准备改变或重新解释既有行为、契约、需求与结构时，先用 Git 和 repo-state 会话历史追查其来源、同类讨论与最终裁定。代码存在不等于有意保留，历史也不覆盖当前事实；证据不足时不得用本文件的通用准则代替产品决定
- 分开写清经验证的当前行为、用户已确认目标和仍属推断的内容，再进入实现设计；用户原则与现行合同有张力时先报告影响等裁定
- 测试姿态：用户要求（含 spec / AC 点名）或修 bug 时才写测试，不主动铺测试套件；先定行为清单（含边界情况）再成组写测试，期望值只来自需求不从实现反推；修 bug 的回归测试必须修复前跑红且失败原因与 bug 一致；实现中冒出的未枚举边界回清单层裁定，不在测试和实现层打补丁续跑；测试断言行为、只走 public interface（深度见 `tdd` skill）
- 新建 API / service / utility 前停靠，到第一层成立就停：本仓库已有 → 语言标准库 / 工具原生 → 已装依赖 → 都没有才新写；新建要写清旧的为什么不够用
- 修改被多处依赖的代码前 grep 调用方；破坏性改动（改签名 / 返回值 / 删方法）列受影响调用方等确认；修 bug 护栏加在共享函数一处，不逐调用点贴补丁
- ≥3 文件或 >15 分钟工作量 → 先出方案（含当前合同的证据、用户目标、未决项、推演出的关键约束与取舍，见铁律 3 / 6）等确认；有回滚风险先记 baseline sha
- 不熟的 CLI / API 先读 `--help` 或 doc，不猜命令和 flag。HuggingFace CLI 前缀是 `hf`（不是 `huggingface-cli`）
- 集成第三方库前先枚举它已提供的相关 API（带文档 / 源码出处），确认没有现成能力覆盖需求再自己写；quickstart 级浏览不算接地，错过深层功能 = 未接地

## 编码克制
> 触发词：try / except / fallback / default / interface / adapter / factory / strategy / 泛型 / config 开关 / validate / guard / lock / 防御性分支——看到自己要写，先过判据再写。
> 管辖范围：目标语义确认后，本节只筛选写进产出物的实现手段（代码 / 文档 / 配置），不能根据触发词重新分类领域合同。发现的设计层问题必须报告，报告不受克制判据约束（铁律 1 / 6）。

- 不预先抽象：不到两个语义相同的真实调用点，不抽函数 / 类 / 接口；不为想象中的未来加扩展点、可选模式、没用上的参数
- 不静默兜底：对已确认属于异常的路径，错误优先抛到能处理的层；不 try/except 吞掉返回默认值，不偷偷换模型 / provider / URL / 解析器。产品合同明确允许的部分结果、跳过或继续执行不是实现层自行定义的 fallback
- 不层层设防：校验只在信任边界（外部输入 / 网络 / 持久化 / 安全 / 并发）做一次；内部函数假设调用方守约，不重复 null 检查和"不可能发生"分支
- 不截断数据：数据路径上不加截断 / 字符上限 / top-N 裁剪，数据量大用流式或分页解决；任何会丢数据的裁剪必须用户点名，并在输出里报告丢了什么
- 不复制状态：新加一层 indirection（canonical 形态 / projection / 中间表示 / 状态副本）前必须指出当前正在失败的例子；一个事实只有一个 source of truth，派生状态能现算就不存副本
- 重构奔新设计：用户已经确认新目标后，按新目标设计、删掉旧路径，不加 adapter / 兼容分支 / 新旧双模式，除非用户点名向后兼容；不得把泛化原则或“重构”二字当作改变旧合同的授权，删旧路径可能破坏数据或外部契约时停下来问
- 理由词红旗："更安全 / 更健壮 / 更灵活 / 未来可能 / 防御性 / 为兼容"若指不出当前在失败的例子、外部边界或用户需求 → 删
- 判据：对已确认应该失败的异常，让程序**停下 / 验证 / 报告 / 大声失败**的护栏留；让异常**静默续跑 / 多接受一种模式 / 多暴露一个开关 / 伺候想象中调用方**的护栏砍。领域合同本身允许的行为不由此判据裁决

## 写完后
- 相关脚本 / 构建 / 测试跑过、退出码确认；对外交付物按 `rules/verification.md` 验收
- 测试内循环：迭代中只跑受影响的测试（单文件 / 单 case），全量 suite 收尾跑一次；分钟级 suite 不进内循环
- UI / 前端行为改动在浏览器边界验证（截图 / browser-use 交互产物），后端 e2e、编译通过、退出码不充数；环境没有浏览器就显式交还"需人工验证"，不拿替代信号冒充
- debug 残留（print / console.log / debugger）清掉；走弯路产生的冗余代码 / 文件 / 死路径自查清理，不留给用户点名
- 过度工程扫描：diff 新增行按「编码克制」节的触发词与判据复扫，指不出真实失败场景 → 撤回。外部审查返回的加固建议按同一判据过滤，不采纳记一句理由；自己当审查者时同一判据管输出侧：审查 = 找有真实失败场景的 bug 和设计层缺陷（方案没经过真实处境推演、在真实处境下站不住，同样是缺陷），不产出防御性加固清单
- 涉及安全 / 权限 / 支付 / 并发：输入校验、权限层级、异常路径、边界条件（空值 / 溢出 / 竞争）过一遍
- commit message 用 Conventional Commits：`<type>[scope]: <描述>`，破坏性改动 type 后加 `!`；repo-state `[doc-ack:]` 等标记放 footer

## 进程与搜索
- 停 dev server / 预览进程禁宽匹配 `pkill` / `killall`：先 `pgrep -f` 列出确认，再 kill 精确 PID（宽匹配曾误杀 MCP server 和自身）；`lsof -ti:PORT` 会把连过该端口的客户端一并列出，停服务器只杀 `-sTCP:LISTEN` 的监听进程
- 长时后台任务用 Bash 工具原生 `run_in_background`（需跨命令持久时配 `dangerouslyDisableSandbox`），禁 `setsid ... &`：沙箱会回收进程，或泄漏成 PPID=1 孤儿副本（曾十几个爬虫副本互抢带宽）；启动后用 no-sandbox `ps` 确认进程数，`pgrep <脚本名>` 会匹配 wrapper 自身误判
- headless 钉 Claude 模型版本用 `claude --model <完整模型ID>`（Agent 工具的 model 参数只收档位别名）；Fable 必须写 `claude-fable-5[1m]`，裸 ID 会 400
- Bash 搜索优先 `rg` 并限定路径 / 文件类型；禁止大目录树宽通配 `grep` / `ugrep`（曾 OOM 弄崩 WSL2）；能用原生 Grep 工具就不开 shell 搜索

## 并发执行
批量同构 + 单元无依赖 + 外部等待主导，三条同时满足 → 默认并发，禁止逐个串行。先按速率约束定并发度 + 退避；命中限流降并发加退避，不退回串行。真实依赖 / 写同一资源 / 纯本地计算 → 串行合理。

## API 脚本与失败阶梯
- 脚本默认：认证预检（key 缺失 / 格式错明确报错退出）；429 / 5xx 指数退避最多 3 次，401 / 403 立即报错不重试；HTTP 超时默认 30s
- LLM API 调用不设短 `max_tokens`（如 4000 一类习惯值）：要么给足够大，要么省略用 provider 默认；用户口中"上下文长度"在 API 调用语境指 `max_tokens` 输出预算，不是服务端 `max_model_len`
- 失败阶梯：先按 `rules/verification.md` 归因；第 1 次失败重试 1 次；第 2 次换**根本不同**的路径（不是调参）并告知用户；第 3 次停，给已验证事实 + 已排除假设 + 选项 + 推荐。`bug-detective` L3/L4 压力档仅在用户对停点回复"继续 / try harder"后解锁
- 瓶颈同走阶梯：连续 2-3 轮迭代无实质提升（没报错不等于在前进）= 非通过；实质提升看剩余关键问题是否缩小，只改表述、分解或中间形态而关键缺口及其验证难度未降，按无提升处理。两问要有证据的"能"（当前思路还正确吗？还能继续提升吗？），答不出就主动转向根本不同的路径 / 角度 / 拆法，不磨也不停摆；在无法提升的方案上继续开发是愚蠢错误的决定

## Subagent 分发
- 每个 prompt 自含任务、输出路径、格式、约束和参考文件，不假设子 agent 知道主会话。必带：用户原话或有出处的目标、经验证的当前行为及证据、用户已确认裁定、仍未决的推断、已否决项、git 安全、自主权边界、受众和方案推演要求。只读调查的任务名与问题表述保持中性，不把待验证假设写成 `failclosed_audit` 这类既定答案；子 agent 发现权威证据冲突时报告，不自行替用户裁决
- 同一未决问题并行派发多个探索任务时，按根本不同的机制或拆法分工；各任务带齐共同事实与用户裁定，但不注入主会话对候选路线的偏好或其他路线的未验证结论，交回后再汇合比较
- 同时 dispatch ≤3；限定只读指定文件、只返回摘要，不把源文件全量拉回；401 / 403 / 503 等 30s 重试 1 次仍失败报告等指示；大仓库调查类超 2 分钟主动中止分段
- 交回契约：会写文件的子任务在 prompt 里指定宿主已有验证器（没有就指定最小检查命令），验证不过不得报 DONE；交回摘要必带 `STATUS:`（DONE / DONE_WITH_CONCERNS / PARTIAL / BLOCKED / NEEDS_INPUT）+ `EVIDENCE:`（词汇同 `references/codex-task-template.md` §9），已验证事实与未验证假设分开列。父会话按 EVIDENCE 复核，不采信 PASS 自评；无 EVIDENCE 按未验证处理
- 委派创造性 / 探索性工作给素材与目标，不规定方法和方向；参考资料标注“仅供参考”，不当强制清单塞给对方
- 后台 agent fire-and-forget：结果等 task-notification 推送再消费，不用 TaskOutput block=true 长超时阻塞等待（会话对用户呈现为卡死）；结果当轮必需且预估很快就直接同步跑

## Codex 调用底线
任何 Codex 委派前先读 `references/codex-delegation.md`（模型路由 / 写模式门槛 / 审查隔离 / 失败处理的唯一权威）。底线：一律走 codex-delegate skill 的 codexctl；model / effort 默认继承 codex config（当前 gpt-5.6-sol / xhigh），不静默降级换模型；长任务用后台 Bash 跑 `--follow` 收果，禁止轮询干等；status 报 STALLED 先归因再 cancel；失败不静默 fallback。
